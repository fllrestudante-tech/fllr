# Benchmarks

Reservado. Vazio hoje -- nenhum benchmark formal foi incorporado ao
Feature Registry ainda (`type: "benchmark"` no
`registry/research-objects.json`).

Quando o cripto10 tiver dado real suficiente pra comparar formalmente
contra um benchmark público (ex: buy-and-hold, um índice, ou os
resultados publicados de um dos projetos auditados em
`research/competitor-intelligence/`), o resultado entra aqui.
